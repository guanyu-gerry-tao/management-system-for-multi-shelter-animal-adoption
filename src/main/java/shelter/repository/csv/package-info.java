/**
 * CSV-backed implementations of the repository interfaces.
 * Each class reads and writes a single {@code .csv} file under {@code ~/shelter/data/},
 * handling serialization, deserialization, and file I/O for one aggregate type.
 */
package shelter.repository.csv;
