/**
 * Presentation layer — Picocli-based CLI entry points for all shelter management commands.
 * Each class in this package defines a command group or subcommand; they parse arguments,
 * delegate to the application layer, and format output for the terminal.
 */
package shelter.cli;
