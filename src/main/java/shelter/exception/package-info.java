/**
 * Domain-specific checked and unchecked exceptions thrown across the service and application layers.
 * Each exception corresponds to a named business rule violation (e.g., shelter at capacity,
 * animal not available) so callers can handle or report errors precisely.
 */
package shelter.exception;
