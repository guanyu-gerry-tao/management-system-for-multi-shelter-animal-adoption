/**
 * Concrete implementations of the application-layer use-case interfaces.
 * Each class in this package wires together one or more service calls to fulfill
 * a single use case without exposing internal service or repository details to callers.
 */
package shelter.application.impl;
