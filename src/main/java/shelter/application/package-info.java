/**
 * Application layer — orchestrates use cases by coordinating domain objects and services.
 * Each interface in this package corresponds to one named use case (UC-01 through UC-08)
 * and defines the contract that the CLI presentation layer depends on.
 */
package shelter.application;
