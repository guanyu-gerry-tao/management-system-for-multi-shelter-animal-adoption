/**
 * Read-only view objects returned by the application layer to the presentation layer.
 * These models decouple the CLI from internal domain representations and carry only
 * the data needed to format output — no business logic lives here.
 */
package shelter.application.model;
