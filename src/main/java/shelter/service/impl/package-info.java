/**
 * Concrete implementations of the service-layer interfaces.
 * Classes here contain the primary business logic for each concern and depend on
 * repository interfaces for persistence, keeping them testable and storage-agnostic.
 */
package shelter.service.impl;
