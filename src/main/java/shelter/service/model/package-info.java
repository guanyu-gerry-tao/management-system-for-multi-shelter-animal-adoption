/**
 * Lightweight result and record types produced and consumed within the service layer.
 * These models carry structured output (match results, audit entries, notification records)
 * across service and application boundaries without exposing mutable domain objects.
 */
package shelter.service.model;
