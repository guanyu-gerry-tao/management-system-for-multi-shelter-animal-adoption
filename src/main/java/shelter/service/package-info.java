/**
 * Service-layer interfaces defining single-responsibility business operations.
 * Each interface encapsulates one concern (adoption lifecycle, matching, vaccination, etc.)
 * and is consumed by the application layer to compose multi-step use cases.
 */
package shelter.service;
