/**
 * Domain layer — core entities and value objects that model the animal adoption problem.
 * Classes here are plain Java objects with no framework dependencies; they encode
 * business invariants and are shared across all upper layers.
 */
package shelter.domain;
